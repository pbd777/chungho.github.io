# ── 의존성 ────────────────────────────────────────────────────────────────────
node_modules/

# ── 빌드 결과물 ───────────────────────────────────────────────────────────────
dist/

# ── 런타임 생성 파일 ──────────────────────────────────────────────────────────
# HLS 스트림 세그먼트 (CCTV 스트리밍 중 생성, 재시작 시 재생성됨)
public/hls/
data/hls/

# 레이아웃 저장 파일 (개인 작업 상태 — 팀 공유 필요 시 이 줄 제거)
# data/layout.json
# data/backups/

# ── 환경 설정 ─────────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── OS / 에디터 ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.vscode/settings.json
.idea/

# ── 로그 ─────────────────────────────────────────────────────────────────────
*.log
logs/
npm-debug.log*

# ── 임시 파일 ─────────────────────────────────────────────────────────────────
*.tmp
*.bak
{src,public

# ── 에셋 (현재: git 포함 / 추후 외부 저장소 전환 시 아래 주석 해제) ────────────
# public/models/*.glb
# public/models/*.gltf
# public/models/*.fbx
# public/models/*.hdr
# public/models/textures/
